//  Time  Complexity is used to measure efficiency of algo in term od speed as input size grow 
//  Time Complexity is != time taken by code to run 
//  formula of binary x = log_2 n
// if iteration is 1000 = it take 10 time 

// means Binary >>> linear in term of time complexity

//  To represent time Complexity we use [Big O notaion]

// # Big O notation : say we measure the time complexity in worse case  

// TYPES OF TIME COMPLEXITY

//  O(n)
// O(log_n)
// O(n^2)
// O(n*log_n)
// O(n^3)
// O(2^n)
// O(n!)

//  CONST TIME COMPLEXITY 
//  mean if size grow it still x=1 Ex : array what ever the value inside the array the time complexity is always 1 like arr[.....n] arr[5] so op is 5 at index 4 it will jump directly